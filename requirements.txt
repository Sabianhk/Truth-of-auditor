# Pin persis versi yang teruji di venv pengembangan (python 3.11) — build
# CI/Railway harus memasang paket yang SAMA, bukan "apa pun yang terbaru".
# pandas dihapus: tidak diimpor di mana pun (dependency mati yang berat).
Django==5.2.15
openpyxl==3.1.5
msoffcrypto-tool==6.0.0
olefile==0.47
pdfplumber==0.11.10
rapidfuzz==3.14.5
python-dateutil==2.9.0.post0

# deployment (Railway/produksi)
gunicorn==26.0.0
whitenoise==6.12.0
psycopg2-binary==2.9.12
dj-database-url==3.1.2
